# 我的日记 · Web 构建环境变量(可选)
# =============================================
# 端点烘焙原则:打包后的 App 端点固定(云服务域名),不依赖用户配置。
# 生产构建时设置 VITE_API_BASE 为你的服务端地址(例如 https://diary.example.com);
# 留空/不设置 = 同源(自带的日记服务,本地/测试用)。

# VITE_API_BASE=https://diary.example.com

# 本地优先构建:桌面端(Tauri)烘焙 =1,数据存本机(IndexedDB),与手机 App 一样;
# 内容只在 PC/手机本地,同步经云端加密中继(服务器不存日记)。
# VITE_LOCAL_FIRST=1
