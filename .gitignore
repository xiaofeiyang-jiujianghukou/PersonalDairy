node_modules/
dist/
*.log
.pnpm-home/
.DS_Store

# 本地配置与密钥(绝不入库)
.env
.env.local
# 构建时的环境配置(端点烘焙,各环境自建;不入库)
.env.production
.env.*.local

# 编辑器交换 / 备份文件(可能残留 .env 内容)
*.swp
*.swo
*~
.~*

# 本地数据(日记属于你一个人)
data/
*.db
*.db-wal
*.db-shm
.android-sdk/

# 本机 Android 构建环境(不入库)
.android-sdk/
.jdk21/
.gradle-home/
.gradle-home-build*.log

# 一键启停脚本的运行时产物
.diary-server.log
.diary.pid
